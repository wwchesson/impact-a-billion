{
    "name": "example-project",
    "description": "Build scripts for Heroku",
    "engines": {
        "node": ">= 14"
    },
    "scripts": {
        "build": "hugo --gc --minify"
    },
    "dependencies": {
        "react-router-dom": "^6.3.0"
    }
}
